extend type Query {
  localState: String!
  complexLocalState: LocalType!
  localServerModel: ServerField!
}

type LocalType {
  someData: String!
}

extend type ServerField {
  addedLocalData: String!
}
